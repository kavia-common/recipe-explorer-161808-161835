package org.example.app.util

/**
 * PUBLIC_INTERFACE
 * Centralized keys used throughout the app (e.g., DataStore).
 */
object Keys {
    const val KEY_EMAIL = "email"
    const val KEY_FAVORITES_SET = "favorites_set"
}
